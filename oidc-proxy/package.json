{
  "name": "oidc-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "vitest": "5.0.0-beta.3",
    "@cloudflare/vitest-pool-workers": "0.18.8"
  }
}
